# Go Personal Project

## Project Overview
This is a personal Go development workspace. It appears to be a newly initialized environment intended for a Go-based monorepo or a single significant Go project.

**Main Technologies:**
- **Language:** Go (Golang)
- **Structure:** Mono-repo attempt (as suggested by `go-personal-mono-repo/` directory)
- **Infrastructure:** Git version control

## Building and Running
*No source code detected yet.*
- **Build:** `go build ./...` (TODO: verify once source is added)
- **Test:** `go test ./...` (TODO: verify once source is added)
- **Run:** `go run main.go` (TODO: verify once entry point is defined)

## Development Conventions
- Follow standard Go project layout (e.g., `/cmd`, `/pkg`, `/internal`).
- Use `go mod` for dependency management.
- Prefer explicit error handling as per Go idioms.

## Project Structure
- `.memsearch/`: Session memory and local indices.
- `.sixth/`: Custom agent skills or configurations.
- `go-personal-mono-repo/`: Targeted directory for the primary codebase.
- `GEMINI.md`: This instruction file.
